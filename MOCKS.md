# Документация по мок-данным (MOCK)

Этот документ содержит список всех мок-данных и заглушек в проекте, которые необходимо удалить или заменить перед деплоем на продакшн.

## ⚠️ ВАЖНО

**Перед деплоем на продакшн необходимо:**
1. Проверить все файлы из списка ниже
2. Удалить или закомментировать мок-данные
3. Убедиться, что приложение работает без моков
4. Протестировать функциональность

---

## 📋 Список файлов с мок-данными

### 1. `src/model/cefi-crypto-deposit-withdraw-assets.ts`

**Тип:** Fallback мок-данные при ошибке запроса

**Описание:** Мок-данные используются как fallback при ошибке получения списка криптоактивов для депозита/вывода.

**Строки:**
- **6-62:** Константа `mockCryptoDepositWithdrawAssets` - массив мок-данных с активами (BTC, ETH, USDT)
- **87-91:** Использование мок-данных в `sample` при ошибке `failData`

**Действие для продакшна:**
- Удалить константу `mockCryptoDepositWithdrawAssets` (строки 6-62)
- Удалить или изменить логику в `sample` (строки 87-91) - либо убрать fallback, либо обработать ошибку иначе
- Убедиться, что при ошибке запроса приложение корректно обрабатывает ситуацию

---

### 2. `src/model/cefi-banking.ts`

**Тип:** Закомментированный мок для тестирования

**Описание:** Мок-данные карты закомментированы, но код остается в файле.

**Строки:**
- **13-75:** Константа `cardInfoMock` - полные мок-данные карты (BasicCardInfo)
- **98-101:** Закомментированный код добавления мок-карты в список активных карт

**Действие для продакшна:**
- Удалить константу `cardInfoMock` (строки 13-75)
- Удалить закомментированный код (строки 98-101)

---

### 3. `src/features/modals/fancy-physical-card/index.tsx`

**Тип:** Закомментированные мок-данные для адреса доставки

**Описание:** Закомментированные мок-данные для адреса доставки и методов доставки карты.

**Строки:**
- **42-62:** Закомментированные мок-данные:
  - `address` объект (строки 43-49)
  - `delivery` массив методов доставки (строки 51-62)

**Действие для продакшна:**
- Удалить закомментированный блок мок-данных (строки 42-62)

---

### 4. `src/features/auth-new/sign-in/index.tsx`

**Тип:** Комментарий-напоминание о моке

**Описание:** Комментарий указывает на использование мок-данных в другом месте.

**Строки:**
- **123:** Комментарий `//MOC $cefiCryptoDepositWithdrawAssets`

**Действие для продакшна:**
- Удалить комментарий (строка 123) или заменить на более информативный, если это необходимо для логики

---

### 5. `src/fideumOTC/model/clients-fideumOTC.ts`

**Тип:** Неиспользуемые тестовые данные

**Описание:** Константа с тестовыми данными клиентов OTC, которая определена, но не используется в коде.

**Строки:**
- **11-30:** Константа `testClientsData` - массив тестовых клиентов OTC

**Действие для продакшна:**
- Удалить константу `testClientsData` (строки 11-30), если она не используется

---

### 6. `src/xanova/features/dashboard-xanova/index.tsx`

**Тип:** Закомментированный мок-изображение

**Описание:** Закомментированное использование мок-изображения графика.

**Строки:**
- **42:** Закомментированный код `{/*<img alt='icon' src={mockChart} />*/}`

**Действие для продакшна:**
- Удалить закомментированную строку (строка 42), если мок-изображение больше не нужно

---

## 🔍 Как найти все моки

Для поиска всех упоминаний моков в проекте используйте:

```bash
# Поиск по ключевым словам
grep -r "MOCK\|mock\|Mock\|MOC" src/ --include="*.ts" --include="*.tsx"

# Поиск TODO с упоминанием моков
grep -r "TODO.*mock\|TODO.*MOCK" src/ --include="*.ts" --include="*.tsx" -i
```

---

## ✅ Чек-лист перед продакшн деплоем

- [ ] Проверен файл `src/model/cefi-crypto-deposit-withdraw-assets.ts`
- [ ] Проверен файл `src/model/cefi-banking.ts`
- [ ] Проверен файл `src/features/modals/fancy-physical-card/index.tsx`
- [ ] Проверен файл `src/features/auth-new/sign-in/index.tsx`
- [ ] Проверен файл `src/fideumOTC/model/clients-fideumOTC.ts`
- [ ] Проверен файл `src/xanova/features/dashboard-xanova/index.tsx`
- [ ] Выполнен поиск по всем файлам на наличие других моков
- [ ] Протестировано приложение без мок-данных
- [ ] Проверена обработка ошибок без fallback на моки

---

## 📝 Примечания

- Некоторые моки могут быть закомментированы и не влиять на работу приложения
- Моки в `cefi-crypto-deposit-withdraw-assets.ts` используются как fallback при ошибках - это может быть желаемым поведением, но требует обсуждения
- После удаления моков убедитесь, что все тесты проходят

---

**Дата последнего обновления:** 2025-01-XX  
**Версия документа:** 1.0
