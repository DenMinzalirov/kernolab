export const regex = {
  pass: /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9]).{8,16}$/,
  email: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
  phone: /^\+\d{1,3}[\s()-]*\d{1,}(\s?[\s()-]*\d{1,}){0,}$/,
} as const
